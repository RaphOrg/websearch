# local postgres (docker-compose)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=websearch
POSTGRES_USER=websearch
POSTGRES_PASSWORD=websearch

# optional: path to a JSONL/CSV file for ingest
INGEST_PATH=./data/documents.jsonl

